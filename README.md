# Workshop Service Workers

## Install dependencies:

```bash
$ npm install
```

## Start project:

```bash
$ npm start
```

This will build the front-end to `/dist`, start up the server, and start a watcher for the javascript.
